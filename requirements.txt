# Core data / numerics
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
matplotlib>=3.7.0

# LLM / embeddings / vector DB
openai>=1.30.0
tiktoken>=0.6.0
qdrant-client>=1.9.0
faiss-cpu>=1.7.4

# PDF processing
PyPDF2>=3.0.0
pdfminer.six>=20221105
PyMuPDF>=1.23.0

# Azure Document Intelligence (used in pdf_processor.py)
azure-ai-documentintelligence>=1.0.0
azure-core>=1.30.0

# Web scraping (scraping_journals.ipynb)
requests>=2.31.0
beautifulsoup4>=4.12.0
selenium>=4.15.0

# Schema validation / data models
pydantic>=2.5.0

# Utilities
python-dotenv>=1.0.0
tqdm>=4.66.0

# Indic language support (Hindi transliteration in Qa_pair_gen.ipynb)
indic-transliteration>=2.3.0
regex>=2023.10.0

# NOTE:
# Qa_pair_gen.ipynb optionally imports the proprietary `fc_ai` package
# and Google Cloud TTS. Those are only required for the specific cells
# that use them and are NOT installed by default. If you need them:
#   pip install google-cloud-texttospeech boto3 fastapi
